on: push
jobs:
  test:
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest, window-lateset]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: action/checkout@v3
      - uses: action/setup-node@v3
        with:
          node-version: 16
      - run: "npm install"
      - run: "npm run build"
